<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eunji.childcycle">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".AdduserActivity"
            android:label="Adduser" />

        <activity
            android:name=".PrepareActivity"
            android:label="PrepareFirst" />

        <activity
            android:name=".PrepareFourthActivity"
            android:label="PrepareFourth" />

        <activity
            android:name=".RidingMainActivity"
            android:label="RidingMain" />

        <activity
            android:name=".FinishRidingActivity"
            android:label="FinishRiding" />

        <activity
            android:name=".ContentsSettingActivity"
            android:label="ContentsSetting" />


        <activity
            android:name=".TempSenseTest"
            android:label="tempSenseTest">

        </activity>

        <activity
            android:name=".bluetooth.BluetoothConnect"
            android:label="BluetoothConnect"/>

    </application>

</manifest>