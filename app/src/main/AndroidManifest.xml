<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eunji.childcycle">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AdduserActivity"
            android:label="Adduser"></activity>

        <activity
            android:name=".LoginActivity"
            android:label="Login"></activity>

        <activity
            android:name=".PrepareFirstActivity"
            android:label="PrepareFirst"
            />

        <activity
            android:name=".PrepareSecondActivity"
            android:label="PrepareSecond"
            />

        <activity
            android:name=".PrepareThirdActivity"
            android:label="PrepareThird"
            />

        <activity
            android:name=".PrepareFourthActivity"
            android:label="PrepareFourth"
            />

    </application>

</manifest>